[project]
name = "uv2nix-dynamic-dependencies-demo"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "dynamic-deps",
]

[tool.uv.sources]
dynamic-deps = { path = "dynamic-deps" }
